/*
yaha hm dekhenge ki operator kya hota hai orpance kya work karta hai  
C language supports a rich set of built-in operators, which can be categorized as follows: 
Arithmetic Operators: Used to perform basic mathematical calculations.

Binary: Addition (+), Subtraction (-), Multiplication (*), Division (/), and Modulus (%).

Unary: Increment (++) and Decrement (--).

Relational Operators: Used to compare two values, returning true (1) or false (0).

Includes: == (Equal to), != (Not equal to), > (Greater than), < (Less than), >= (Greater than or equal to), and <= (Less than or equal to).
Logical Operators: Used to combine multiple conditions or perform logical negation.

Includes: && (Logical AND), || (Logical OR), and ! (Logical NOT).

Bitwise Operators: Perform operations at the bit level on integer values.

Includes: & (AND), | (OR), ^ (XOR), ~ (Complement), << (Left Shift), and >> (Right Shift).
Assignment Operators: Used to assign values to variables.
Simple: =.
Compound: +=, -=, *=, /=, %=, &=, |=, ^=, <<=, >>=.
Conditional (Ternary) Operator: A shorthand for if-else that operates on three operands.
Syntax: condition ? expression1 : expression2;.
Special/Miscellaneous Operators: Specific symbols for memory and data type tasks.
sizeof(): Returns the size of a data type in bytes.
& (Address-of) and * (Dereference): Essential for Pointer manipulation.
, (Comma): Separates and evaluates multiple expressions.
. (Dot) and -> (Arrow): Used to access members of structures or unions.
*/